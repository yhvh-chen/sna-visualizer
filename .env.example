# Example environment variables for local development
# Copy this file to `.env` and fill in real values before running the application.

# --- GeoIP / MaxMind ---
# If you have a MaxMind license and want the app to auto-download the GeoLite DB,
# set MAXMIND_LICENSE_KEY and enable DOWNLOAD_GEOLITE=true. Otherwise place the
# DB file at GEOLITE_DB_PATH and leave DOWNLOAD_GEOLITE=false.
MAXMIND_LICENSE_KEY=xxxxxx
GEOLITE_DB_PATH=./GeoLite2-City.mmdb
DOWNLOAD_GEOLITE=false

# --- SMC (Stealthwatch) connection ---
# Used to pre-fill the GUI connection form. Keep credentials out of source control.
SMC_HOSTNAME=your.smc.host
SMC_USERNAME=youruser
SMC_PASSWORD=yourpass
SMC_VERIFY_SSL=false

# --- Application settings ---
OUTPUT_DIR=./output
FLASK_ENV=development
SECRET_KEY=change_me_for_production

# --- Runtime / tuning ---
# Default HTTP request timeout (seconds) used for downloads / API calls
TIMEOUT_SECONDS=30
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
